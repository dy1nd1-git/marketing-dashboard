{
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4",
    "tailwindcss": "^4.3.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --max-warnings=0",
      "vitest related --run"
    ]
  }
}
